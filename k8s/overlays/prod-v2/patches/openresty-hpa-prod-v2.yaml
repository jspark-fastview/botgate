# prod — baseline 2대 평소 (min=2), spot fallback 으로 트래픽 폭증 시 scale up 가능 (max=10).
# nodeAffinity preferred 라 spot 도 OK, baseline 가용 시 우선.
- op: replace
  path: /spec/minReplicas
  value: 2
- op: replace
  path: /spec/maxReplicas
  value: 10
