# ArgoCD UI 외부 도메인 — argocd.viewus.co
# *.viewus.co wildcard 인증서 자동 attach (ALB SNI discovery).
apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: argocd

resources:
  - ingress.yaml
  - cmd-params.yaml
