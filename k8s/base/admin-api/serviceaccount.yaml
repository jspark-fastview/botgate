# Pod Identity — Terraform 의 aws_eks_pod_identity_association.admin_api 가 매핑
# (namespace=guardus, sa=admin-api → role guardus-admin-api).
# manifest 에 ARN annotation 없음 — drift 방지.
apiVersion: v1
kind: ServiceAccount
metadata:
  name: admin-api
