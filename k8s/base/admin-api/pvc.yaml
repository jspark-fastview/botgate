apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: guardus-db
spec:
  # SQLite — 단일 노드 attach.
  # 노드 장애 시 EBS 가 같은 AZ 의 다른 노드로 자동 detach/attach
  accessModes:
    - ReadWriteOnce
  storageClassName: gp3
  resources:
    requests:
      storage: 10Gi
