apiVersion: v1
kind: Service
metadata:
  name: internal-api
  labels:
    app.kubernetes.io/name: internal-api
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: internal-api
  ports:
    - name: http
      port: 3001
      targetPort: http
